// Use the sort method in the alphabetalOrder function to sort the elements of arr in alphabetical order.
// The fnction should return the sorted array.

function alphabetalOrder(arr) {
    return arr.sort();
}