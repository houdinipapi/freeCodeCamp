/**
 * Use the Dog constructor to create a new instance of Dog, assigning it to a variable hound.
 * hound should be created using the Dog constructor.
 * Your code should use the new operator to create an instance of Dog.
 */

function Dog() {
    this.name = "Spot";
    this.color = "brown";
    this.numLegs = 4;
}

let hound = new Dog();