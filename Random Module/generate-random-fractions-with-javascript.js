/**
 * Math.random() function generates a random decimal number btwn 0 (inclusive) and 1 (exclusive).
 * the function can return a 0 but never a 1
 */

function randomFraction() {

  return Math.random();
}